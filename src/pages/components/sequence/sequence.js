import React from 'react';

const sequence = ({
    sequence,
    format
}) => {
    switch (format) {
        case 'fasta':
            return ( 
                <>
                </>
             );
        default:
            return ( 
                <>
                </>
             );
    }
}
 
export default sequence;