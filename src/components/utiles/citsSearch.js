
export default function(allCitations, citations, text){
    let cits =''
    try {
        //let cits = new RegExp(/\|.*?\|/, text)
    console.log(cits)
    } catch (error) {
        console.log("util")
    }
    
}